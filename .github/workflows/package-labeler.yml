name: Workspace Package Labeler
on: [pull_request]

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: csi-lk/package-labeler@v1.0.0
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          workspace: "packages"
          name: "folder"
          prefix: "pkg: "
